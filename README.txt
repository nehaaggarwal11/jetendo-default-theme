The Jetendo CMS Themes are meant to be distributed as separate Git Repositories so that the core project doesn't need to track versioning in the themes directory.

This project is automatically checked out when installing Jetendo CMS.
